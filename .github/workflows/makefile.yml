name: Makefile CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.15'
        
    - name: Use Node.js 11
      uses: actions/setup-node@v3
      with:
        node-version: '11'

    - name: test backend
      run: cd aether/backend; go run main.go

    - name: Build the frontend
      run: cd ../client; npm ci; npm run wpbuild; npm run rebuild

    - name: Install dependencies
      run: cd ..; make

    - name: Build linux
      run: make linux

    - name: Build windows
      run: make win
      
    - name: Build mac
      run: make mac
      
    - name: Test
      run: bash run-test.sh
